<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp - New URL</title>
  <style>
    body {
      background-color: #f9f8ff;
    }
    .btn-link {
      color: #767676;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 12px;
    }
    .btn-link:hover {
      color: #563d7c;
      text-decoration: none;
    }
    .header-link {
      color: #fff;
      opacity: .5;
      transition: opacity 0.5s;
      display: inline-block;
      margin-left: 20px;
    }
    .header-link:hover {
      color: #fff;
      opacity: 1;
      text-decoration: none;
    }
    .btn-warning {
      color: #fff;
      background-color: rgba(255,255,255,0.25);
      border-color: rgba(255,255,255,0.25);
      transition: background-color 0.5s, border-color 0.5s, color 0.5s;
    }
    .btn-warning:hover {
      color: #259eff;
      background-color: #fff;
      border-color: #fff;
    }
  </style>
</head> <!-- End Head -->
<body>

  <header>
    <% include partials/_header %>
  </header> <!-- End Header Include -->

  <div style="max-width:650px; margin:0 auto;">
    <h3>New TinyURL</h3>

    <div class="row no-gutters">
      <div class="col-md-12" style="background-color:#fff; border:solid 2px #eeeffa; border-radius:4px; padding:9px 14px 5px">
        <form method="POST" class="form-inline" action="/urls">
          <div class="form-group mb-2">
            <label for="longURL">Enter a URL:</label>
            <input class="form-control" type="text" name="longURL" placeholder="http://" style="width: 420px; margin: 1em">
            <button type="submit" class="btn btn-success">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </div>

</body> <!-- End Body -->
</html>