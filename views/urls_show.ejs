<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp - shortURL: <%= shortURL %></title>
  <style>
    .btn-link {
      color: #767676;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 12px;
    }
    .btn-link:hover {
      color: #563d7c;
      text-decoration: none;
    }
    .header-link {
      color: #cdbfe3;
      transition: color 0.5s;
      display: inline-block;
      margin-left: 20px;
    }
    .header-link:hover {
      color: #fff;
      text-decoration: none;
    }
    .btn-warning {
      color: #6f5499;
      background-color: #cdbfe3;
      border-color: #ac9ac8;
    }
    .btn-warning:hover {
      color: #cdbfe3;
      background-color: #6f5499;
      border-color: #cdbfe3;
    }
    .create-new-link {
      color: #767676;
      font-size: 13px;
      font-weight: 500;
      display: inline-block;
      margin-bottom: 20px;
    }
    .create-new-link:hover {
      color: #563d7c;
      text-decoration: none;
    }
  </style>
</head> <!-- End Head -->
<body>

  <header>
    <% include partials/_header %>
  </header> <!-- End Header Include -->

  <div style="max-width:650px; margin:0 auto;">

    <a class="create-new-link" href="/urls/new">Create new TinyURL</a>
    
    <h2>TinyURL => <%= shortURL %></h2>
    

    <div class="row no-gutters">
      <div class="col-md-12" style="background-color: #fff; border: solid 1px #ddd; border-radius: 4px; padding:9px 14px 5px">
        <form method="POST" class="form-inline" action="/urls/<%= shortURL %>">
          <div class="form-group mb-2">
            <input class="form-control" type="text" name="newurl" placeholder="<%= longURL %>" style="width: 510px; margin: 1em">
            <button type="submit" class="btn btn-success">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </div>

</body> <!-- End Body -->
</html>