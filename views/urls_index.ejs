<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
  <style>
    body {
      background-color: #f9f8ff;
    }
    .btn-link {
      color: #2a2e3f;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 12px;
    }
    .btn-link:hover {
      color: #3166ff;
      text-decoration: none;
    }
    .header-link {
      color: #fff;
      opacity: .5;
      transition: opacity 0.5s;
      display: inline-block;
      margin-left: 20px;
    }
    .header-link:hover {
      color: #fff;
      opacity: 1;
      text-decoration: none;
    }
    .btn-warning {
      color: #fff;
      background-color: rgba(255,255,255,0.25);
      border-color: rgba(255,255,255,0.25);
      transition: background-color 0.5s, border-color 0.5s, color 0.5s;
    }
    .btn-warning:hover {
      color: #259eff;
      background-color: #fff;
      border-color: #fff;
    }
    .create-new-link {
      color: #8e8fa6;
      font-size: 13px;
      font-weight: 500;
      display: inline-block;
      margin-bottom: 20px;
      transition: color 0.5s;
    }
    .create-new-link:hover {
      color: #3c3c4a;
      text-decoration: none;
    }
  </style>
</head> <!-- End Head -->
<body>

  <header>
    <% include partials/_header %>
  </header> <!-- End Header Include -->

  <div style="max-width:650px; margin:0 auto;">
    
    <a class="create-new-link" href="/urls/new">Create a new TinyURL</a>
  
    <h2>My TinyURL's</h2>

    <div class="row no-gutters">
      <div class="col-md-12" style="background-color:#fff; border:solid 2px #eeeffa; border-radius:4px; padding:9px 14px 5px">
        <div class="row">
          <% for(let shortURL in urls) { %>
            <div class="col-md-9" style="padding:6px 15px; flex:0 0 calc(100% - 166px); max-width:100%;">
              <p style="margin-bottom:0;"><%= shortURL %>&nbsp;&nbsp;&nbsp;&nbsp;=>&nbsp;&nbsp;&nbsp;&nbsp;<%= urls[shortURL] %></p>
            </div>
            <div class="col-md-3" style="flex:0 0 166px; max-width:100%; padding-bottom:4px;">
              <a href="/urls/<%= shortURL %>" class="btn btn-link">Edit</a>
              <form style="display:inline-block;" method="POST" action="/urls/<%= shortURL %>/delete">
                <button class="btn btn-danger" type="submit">Delete</button>
              </form>
            </div>
            <br />
          <% } %>
        </div>
      </div>
    </div>
  </div>

</body> <!-- End Body -->
</html>

<!-- style="background-color: #fff; border: solid 1px #ddd; border-radius: 4px;" -->